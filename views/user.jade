extends layout

block content
  if user
      a.btn.btn-link(href='/users/logout') Logout
  else
      a.btn.btn-link(href='/users/login') Login
      br
      a.btn.btn-link(href='/users/signup') Create Account
